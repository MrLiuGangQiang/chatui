# ChatUI v2.0.7

## 变更

- 将欢迎页、公告和模型配置建议统一为使用 `gpt-5.6-terra` 作为意图识别模型；保留 DeepSeek 兼容路径，但不再将其作为推荐模型。
- 优化意图识别提示词的证据优先策略，以及关系、图片任务和资源绑定边界，减少错误路由与资源误绑定。
- 修复短的联网搜索追问丢失上一轮主题的问题，使用路由模型解析后的搜索目标执行。
- 对超过 50 个独立图片结果的请求在调用图片规划器前失败关闭，并提示用户分批处理。
- 增加模型推荐、搜索追问、图片数量上限和提示词契约的回归测试。
