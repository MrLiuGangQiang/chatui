# ChatUI v2.1.35

## Changes

- 补充并冻结 200 synthetic Chat Job 的专项容量门禁：覆盖 200 个独立 SSE response、单浏览器 200 Job 分片/轮询恢复，以及上游并发准入边界。
- 将专项容量测试加入 `npm run test:streaming`、`npm run check` 和 CI，避免只凭单元路径通过就宣称 200 并发生产可用。

## Verification

- `npm run check`：项目、架构、语法检查及 2058 个测试通过。
- `npm run test:streaming`：41 个流式、恢复、背压、批量清理和 200 synthetic Job 测试通过。
- GitHub main CI：Node 20.19、Node 22 和 Exact Docker runtime 均通过。
- 发布候选必须继续通过 immutable candidate digest runtime、ACR/Docker Hub 同 digest、GitHub Release 和 tag workflow 验证。
