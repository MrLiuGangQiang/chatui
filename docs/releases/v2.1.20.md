# ChatUI v2.1.20

## 修复

- 修复长任务等待提示“已等待 N 秒”只在切换会话或页面时刷新一次的问题；等待期间现在会持续原位更新。

## 改进

- 上游超时改为空闲超时：持续收到响应数据会重置计时，不再因为请求总时长达到阈值而中止。
- 默认只有在连续 600 秒完全没有收到响应数据时，才判定上游超时。
- 高频流式输出不会逐 chunk 重建超时定时器，避免额外性能开销。

## 验证

- `npm run check`：通过
- 1940 个测试、338 个测试文件：通过
