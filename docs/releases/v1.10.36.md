# ChatUI v1.10.36

## Changes

- Improve Markdown render-cache memory control for long conversations by enforcing the configured content budget.
- Reduce repeated synchronous reads and hashing when generating static JavaScript and CSS bundle revisions.
- Ensure updated precompressed static assets receive fresh ETags instead of stale `304 Not Modified` responses.
- Add focused regression tests for cache limits, bundle fingerprint reuse, and precompressed asset validation.
