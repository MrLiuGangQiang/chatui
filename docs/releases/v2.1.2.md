# ChatUI v2.1.2

ChatUI v2.1.2 更新了应用图标：换成了全新 ChatUI 品牌形象，并修复了浏览器标签页图标不刷新的缓存问题。

## 品牌图标更新

- 站点图标（favicon）换成全新 ChatUI 品牌形象（蓝紫渐变圆角 + 机器人头像 + 「ChatUI」字样），透明背景、128×128 高清降采样。
- 修复浏览器标签页图标缓存：favicon 引用改为 `./favicon.svg?v=2`，浏览器会把缓存中的旧图标视为过期并重新拉取，无需手动清缓存。

## 稳定性与质量

- `npm run check` 全量通过。
