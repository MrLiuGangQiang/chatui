# ChatUI v1.10.51

## 修复

- 修复历史消息重新生成时的流式滚动：内容更新和末尾定位现在在同一次渲染事务中完成，不再先按旧高度滚动、再在下一帧纠正；流式消息下方的历史内容保持稳定，不再反复闪动或跳动。
- 修复“继续查看输出”：首次点击直接把当前正在流式输出消息的**末尾**对齐到可见输出区域；后续点击不会再触发第二次校正。
- 增加流式滚动事务回归测试，防止恢复内容更新前的预滚动或 RAF 延迟补偿逻辑。
