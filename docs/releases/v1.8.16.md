# ChatUI v1.8.16

This hotfix makes large image job results recover through the query endpoint.

- Fall back from an oversized SSE completion event to the job query API.
- Avoid reporting a completed image generation as a terminal failure when its base64 result exceeds the event buffer.
