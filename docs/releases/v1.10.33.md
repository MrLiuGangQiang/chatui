# ChatUI v1.10.33

## Changes

- 修复多图逐张重生成被错误编译为普通参考图生成的问题。
- 多图编辑现在为每个选定目标创建独立的 `edit_image` 子任务，并保留正确的目标图片绑定。
- 修复多图 batch 执行资源投影，避免不同子任务复用同一张图片或丢失图片输入。
- 修复会话消息异步写入导致不同会话内容串联的问题。
- 更新图片任务依赖注入和静态入口缓存版本，降低浏览器继续使用旧代码的风险。
