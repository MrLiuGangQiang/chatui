# ChatUI v1.7.31

## Fixes

- Quoted-message image generation now accepts valid `task_contract.v3` message references instead of incorrectly reporting an invalid task structure.
- The selected quoted message body is combined with the current image instruction, while stale, mismatched, or empty references fail safely.
- Quoted message and image bindings now remain tied to the exact selected history item across routing and contract repair.

## Verification

- Ran `npm run check`: project, architecture, syntax, and all 474 automated tests passed.
