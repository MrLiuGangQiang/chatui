# ChatUI v1.10.52

## 修复

- 流式回复改为仅更新当前正在输出消息的局部 DOM；已就位的消息不再在每个 token 中从消息列表移除并重新插入，避免下方历史消息闪烁。
- “继续查看输出”首次点击现在同时完成末尾定位和持续锚定；后续 token、思考内容及高度变化会继续以同一条流式消息的末尾为基准。
- 新生成、重新生成和编辑重发统一收敛到同一套流式渲染与滚动提交规则，并新增局部渲染、首次点击持续锚定和入口收敛回归测试。
