# ChatUI 架构审查、性能优化与测试提示词

> 使用方式：将本文件中“提示词正文”部分完整复制给 AI 编程助手。建议先执行审查阶段，确认结果后再执行实施阶段。

## 提示词正文

请对当前 ChatUI 仓库进行一次全面、深入且可验证的代码审查，并直接修复确认存在的问题。

本次工作的核心目标是：

1. 判断当前架构是否清晰、合理且便于长期维护；
2. 找出真实的性能、可靠性、安全性和可测试性问题；
3. 修复问题的根本原因，而不是掩盖症状；
4. 为每个修复增加专门的自动化回归测试或确定性质量门禁；
5. 在不破坏现有功能、接口、部署方式和运行时行为的前提下完成优化；
6. 最终通过 ChatUI 仓库规定的全部检查。

不要只给建议。如果问题能够被可靠证明并安全修复，请直接修改代码、补充测试并执行验证。

---

## 一、必须遵守的仓库规则

开始工作前，先读取并严格遵守：

- `AGENTS.md`
- `README.md`
- `docs/architecture.md`
- `package.json`
- CI workflow
- Dockerfile、容器启动及发布验证脚本
- 测试配置和现有测试目录

以下规则不可违反：

1. 保持 `docs/architecture.md` 定义的浏览器端、服务端和共享代码边界。
2. 不得将仅适用于 Node.js 的模块引入浏览器代码。
3. 不得将浏览器 API 泄漏到服务端或共享代码。
4. 保留根目录静态入口资源，除非静态服务器、Docker 镜像、测试和文档被同步更新。
5. 新增测试应放在 `test/unit/` 或 `test/smoke/`。
6. 不要扩展 `test/legacy/`，除非是在维护已经存在的历史回归测试。
7. 每个修复必须有独立、明确的质量门禁，用来防止该问题再次发生。
8. 新增质量门禁必须接入 `npm run check` 和 CI。
9. 修改结束后必须运行 `npm run check`。
10. 不得通过删除测试、放宽断言、忽略错误或降低检查标准让测试通过。
11. 不要提交生成报告、日志、临时文件、本地编辑器状态或敏感信息。
12. 未经明确要求，不要执行 `git commit`、`git push`、创建 Git 标签、GitHub Release 或发布 Docker 镜像。

---

## 二、保护当前工作区

在修改前执行并检查：

```powershell
git status --short
git branch --show-current
git log -1 --oneline
```

要求：

1. 识别当前未提交修改。
2. 不得覆盖、删除、回退或重新格式化不属于本次任务的修改。
3. 不得使用 `git reset --hard`、`git checkout -- .`、`git restore .` 或 `git clean -fd` 清除用户工作。
4. 如果工作区已有修改，分析其归属，并让本次修改保持最小范围。
5. 不要将已有修改错误地宣称为本次完成的工作。

---

## 三、建立修改前基线

在修改代码前：

1. 检查 `package.json` 中定义的所有质量检查脚本。
2. 运行 `npm run check`，建立修改前基线。
3. 如果完整检查耗时较长，也必须先运行与目标代码直接相关的测试。
4. 记录已存在的失败、失败命令、错误信息，以及是否与当前工作区修改有关。
5. 不要把修改前已经存在的失败算成本次修改引起的失败。
6. 不得虚构命令执行结果。

---

## 四、理解 ChatUI 的实际架构

### 4.1 浏览器端

检查：

- 页面入口和初始化过程
- 对话状态管理
- 消息列表更新与渲染
- 消息流式输出
- Markdown 渲染
- 代码块和语法高亮
- 用户输入与发送流程
- 会话切换
- 请求取消
- 错误提示
- 本地存储
- DOM 事件绑定和释放
- 浏览器资源加载

### 4.2 服务端

检查：

- 静态资源服务
- API 路由
- 上游模型/API 请求
- 流式响应转发
- 中断和超时处理
- 客户端断开后的资源释放
- 配置读取
- 版本信息接口
- 错误状态码
- 日志及敏感信息处理
- 服务启动和关闭流程

### 4.3 共享代码

检查：

- 浏览器端与服务端共享的数据结构
- 请求和响应格式
- 配置契约
- 版本元数据
- 错误格式
- 模块边界
- 是否错误依赖某一运行时的全局对象或内置模块

### 4.4 构建和运行

检查：

- npm scripts
- 静态文件入口
- Dockerfile
- `.dockerignore`
- 容器启动命令
- 健康检查
- `/api/version`
- 运行时版本和 Git SHA
- runtime source fingerprint
- CI 与本地检查是否一致
- Docker 中实际运行的内容是否与源代码候选一致

必须依据真实代码、调用链和测试形成结论，不要仅凭目录或文件名猜测。

---

## 五、重点架构审查

请寻找并验证以下问题：

1. 浏览器、服务端和共享层之间是否存在越界依赖。
2. UI、状态管理、网络请求和数据转换是否过度耦合。
3. 是否存在职责过多的模块或超长函数。
4. 是否存在重复实现的请求、错误处理、配置解析或消息转换逻辑。
5. 业务规则是否散落在 DOM 事件或渲染代码中。
6. 流式响应的解析、状态更新和视图更新是否合理分层。
7. 是否存在循环依赖或隐式初始化顺序。
8. 是否依赖可变全局状态。
9. 公共接口是否明确、稳定且容易测试。
10. 错误处理是否统一，是否存在异常被吞掉的情况。
11. 配置默认值、运行时环境变量和前端配置是否来源明确。
12. 构建产物、源码、容器镜像和版本元数据是否能够对应。
13. 是否存在为了“看起来高级”而引入的不必要抽象。

评价架构时，同时考虑当前项目规模、改动成本、实际收益、兼容风险、测试难度和后续维护成本。不要为了套用设计模式而进行无收益重构。

---

## 六、重点性能审查

### 6.1 前端性能

重点检查：

1. 流式响应每收到一个 token 是否触发昂贵的完整 Markdown 渲染。
2. 是否反复重建完整消息列表或大段 DOM。
3. 是否存在频繁同步布局、强制 reflow 或布局抖动。
4. 是否对相同 Markdown、代码高亮或消息内容重复计算。
5. 长会话下是否出现明显的时间复杂度增长。
6. 滚动、输入和 resize 事件是否缺少合理调度。
7. EventListener、Observer、定时器和 AbortController 是否正确清理。
8. 已离开会话的流式请求是否仍在更新界面。
9. 是否重复读写 `localStorage`。
10. 是否将大对象或完整响应长期保留在闭包中。
11. 静态资源是否重复加载或阻塞首屏。
12. 是否存在不必要的深拷贝、序列化和反序列化。

### 6.2 服务端性能

重点检查：

1. 请求热路径中是否存在同步文件操作。
2. 是否为每个请求重复读取或解析不变配置。
3. 流式响应是否真正流式转发，而不是完整缓冲后再发送。
4. 是否正确处理背压。
5. 客户端断开后，是否终止上游请求并释放资源。
6. 超时、重试和取消是否可能导致请求堆积。
7. 是否存在未清理的连接、流、监听器或定时器。
8. 是否向日志写入过大的请求体或响应体。
9. 静态资源缓存头是否合理且不会造成版本更新错误。
10. 健康检查或版本接口是否执行不必要的昂贵操作。

### 6.3 性能证据

禁止进行无依据的性能优化。每个性能问题必须提供：

- 文件及位置
- 相关调用链
- 触发条件
- 修改前行为
- 可重复的测量方法
- 修改后的验证结果
- 复杂度、调用次数、耗时或内存变化
- 可能引入的权衡

优先采用稳定断言，例如渲染调用次数、DOM 更新次数、配置读取次数、上游取消次数、事件监听器数量、流是否被完整缓冲和算法复杂度变化。避免将容易受到机器性能影响的绝对毫秒数作为唯一质量门禁。

如果没有可靠数据，明确写“尚无可靠性能数据”，不要虚构提升比例。

---

## 七、可靠性和安全审查

重点检查：

1. 用户输入是否经过正确验证和编码。
2. Markdown 和 HTML 渲染是否可能造成 XSS。
3. 外部链接是否存在不安全跳转或 opener 风险。
4. API 参数是否可以导致注入、路径遍历或 SSRF。
5. 代理到上游服务时是否允许访问不应访问的地址。
6. 错误响应中是否泄漏密钥、令牌、请求头或内部堆栈。
7. 日志中是否记录敏感对话内容或认证信息。
8. 客户端断开是否正确传播到上游 AbortSignal。
9. 并发发送、重新生成、停止生成和切换会话是否存在竞态。
10. 迟到的旧响应是否可能覆盖新会话状态。
11. JSON、SSE 或其他流式数据遇到半包、粘包、非法数据时是否可靠。
12. 流式 UTF-8 多字节字符跨 chunk 时是否正确解码。
13. 上游非 2xx、超时、中断和网络错误是否产生正确状态。
14. 服务关闭时是否清理连接和资源。
15. 不可信配置是否可能进入前端或响应。

不要在报告、测试输出或日志中显示真实密钥。

---

## 八、问题分类和修改策略

将发现的问题分为：

- **P0**：安全事故、数据损坏、严重越权、稳定崩溃
- **P1**：明确的可靠性缺陷、竞态、资源泄漏或真实性能瓶颈
- **P2**：重要架构、维护性和测试性问题
- **P3**：收益有限的代码质量改善

每个问题必须说明：

1. 文件与位置；
2. 根本原因；
3. 真实影响；
4. 触发方式；
5. 证据或复现方法；
6. 推荐修复；
7. 修改风险；
8. 回归测试方案。

优先实施证据充分、收益明确、风险可控、可以自动验证且不破坏兼容性的修改。不要一次性重写 ChatUI，也不要混入大量无关清理。

---

## 九、实施要求

对确认的问题直接完成修复，但必须遵循：

1. 修复根本原因，不接受特殊分支绕过、静默吞错或禁用功能。
2. 保持现有用户可观察行为。
3. 保持 API 请求和响应格式兼容。
4. 保持配置兼容。
5. 保持静态资源路径和入口兼容。
6. 保持 Docker 部署方式兼容。
7. 不要无理由增加新依赖。
8. 不要随意升级依赖。
9. 不要进行无关格式化、批量重命名或目录移动。
10. 修改应保持小而可审查。
11. 如果大型重构无法在一个安全变更中完成，只记录问题和分阶段方案，不要强行实施。
12. 若架构边界或使用方式发生变化，同步更新 `docs/architecture.md` 和相关文档。

---

## 十、每个修复必须有独立质量门禁

对于每一个修复：

1. 在 `test/unit/` 或 `test/smoke/` 中添加专门的测试或确定性验证。
2. 测试名称必须能清楚描述原失败模式。
3. 证明测试在没有修复时能够失败。
4. 证明修复后测试通过。
5. 将测试接入 `npm run check`。
6. 确认 CI 会执行该质量门禁。

建议覆盖正常路径、错误路径、空输入、非法输入、边界条件、并发操作、请求取消、客户端断开、分块流式数据、UTF-8 跨 chunk、迟到响应、资源清理、长会话和重复初始化。

不允许：

- 删除失败测试；
- 降低断言；
- 增加无意义超长超时；
- 使用 sleep 掩盖竞态；
- 跳过测试；
- 仅验证“没有抛异常”；
- 只验证代码能编译。

---

## 十一、本地服务端口规则

ChatUI 本地开发和人工测试只能使用端口 `8765`。

启动服务前，必须检查端口：

```powershell
Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue
```

如果端口已占用：

1. 查明监听进程 PID、进程名称和命令行；
2. 判断是否为当前工作区的 ChatUI 实例；
3. 如果是，可以复用；
4. 如果不是，报告冲突；
5. 不得自动切换到其他端口；
6. 不得停止未经确认的进程。

只有确认监听器属于当前 ChatUI 工作区，或用户明确要求停止时，才可以终止该进程。

---

## 十二、验证顺序

每完成一项修复：

1. 运行新增的专门回归测试；
2. 运行受影响模块的现有测试；
3. 运行相关 lint 或类型检查；
4. 检查 Git diff，排除无关修改。

全部修改完成后运行：

```powershell
npm run check
```

如果 Docker 在本地可用，并且修改影响服务端运行时、静态资源打包、Dockerfile、启动脚本、版本接口或容器环境配置，则运行：

```powershell
npm run preview:release
```

如果 Docker 不可用，明确记录未执行的命令、原因和仍然存在的验证风险。

---

## 十三、最终报告格式

### 1. 总体结论

- ChatUI 当前架构成熟度
- 主要优点
- 最大风险
- 本次优化的核心收益

### 2. 架构说明

简要说明：

- 浏览器端职责
- 服务端职责
- 共享代码职责
- 数据与流式响应路径
- 构建及 Docker 运行路径

### 3. 问题清单

按 P0、P1、P2、P3 排序，每项包含：

- 文件和代码位置
- 根本原因
- 触发条件
- 影响
- 证据
- 修复状态

### 4. 已完成修改

每项说明：

- 修改文件
- 修改逻辑
- 为什么属于根因修复
- 是否影响 API 或用户行为
- 对应的回归测试

### 5. 性能验证

使用表格列出：

| 场景 | 修改前 | 修改后 | 改善 | 测量方法 |
|---|---:|---:|---:|---|

如果无法可靠测量，明确说明，不要虚构数据。

### 6. 测试结果

逐条列出实际执行的命令及结果：

- 专门回归测试
- 单元测试
- smoke test
- lint
- 类型检查
- 构建
- `npm run check`
- `npm run preview:release`（如果适用）

包含通过数、失败数及关键结果。只有实际执行成功的命令才能标记为通过。

### 7. 工作区变更

列出：

- 新增文件
- 修改文件
- 删除文件
- 修改前已经存在、但不属于本次工作的变更

### 8. 剩余问题

列出：

- 尚未处理的问题
- 风险等级
- 未处理原因
- 推荐的后续步骤

### 9. 风险与人工验证

说明：

- 兼容风险
- 部署风险
- 推荐的人工测试场景
- 回滚方法

---

## 十四、最终约束

1. 不要声称能够“保证绝对不出问题”。
2. 应通过自动化测试、静态检查、构建、容器验证和人工测试建议，把风险降到合理范围。
3. 不要虚构问题、测试结果或性能数据。
4. 如果没有发现可证明的问题，不要为了显示工作量而修改代码。
5. 如果检查失败，不要报告工作已完成。
6. 如果出现与本次修改无关的既有失败，清楚区分并提供证据。
7. 未经明确要求，不要提交或发布代码。
8. 最终保留一个范围明确、容易审查且有完整测试保护的变更集。

---

## 推荐：第一阶段只审查

首次执行时，可在提示词末尾追加：

```text
本轮只执行分析和验证，不修改任何文件。

请输出：

1. ChatUI 架构图或文字调用链；
2. 按 P0～P3 排序的问题清单；
3. 每个问题的代码证据和复现方式；
4. 建议的最小修复方案；
5. 对应的回归测试设计；
6. 建议的实施顺序；
7. 预计修改的文件列表。

不得仅凭主观判断报告问题。确认问题后，等待我批准实施计划。
```

## 推荐：第二阶段实施

审查结果确认后，发送：

```text
请根据刚才确认的审查结果开始实施。

要求：

1. 先处理 P0 和 P1；
2. 每次只处理一个逻辑相关的问题；
3. 每个修复必须包含独立回归测试；
4. 每项修复后先运行针对性测试；
5. 全部修改结束后运行 npm run check；
6. 涉及容器运行时的修改，在 Docker 可用时运行 npm run preview:release；
7. 不要处理未经证实的问题；
8. 不要执行 git commit、git push、打标签或发布；
9. 最终按约定格式报告实际修改和验证结果。
```
