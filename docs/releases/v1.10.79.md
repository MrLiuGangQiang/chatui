# ChatUI v1.10.79

## 新功能

- 新增实时在线人数统计：右上角显示「🟢 N 人在线」徽标，每个打开的标签页算一个在线用户，人数变化实时刷新。
- 每个标签页使用独立标识，刷新页面不重复计数，关闭标签页自动下线；网络断开后自动重连。
- 服务端自动清理假死连接并定时发送心跳，保证人数统计长期准确、不会虚高。
- 新增 `/api/presence`、`/api/presence/stream`、`/api/presence/heartbeat` 接口，单实例部署可直接使用，后续可扩展为多实例共享计数。