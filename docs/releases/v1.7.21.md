# ChatUI v1.7.21

Released on July 23, 2026.

## Fixes

- Fixed Markdown table headers using browser-default centering when a column uses the normal `---` divider.
- Default table columns now explicitly use left alignment in streaming previews, large-message previews, and completed Markdown output.
- Added regression coverage to keep header and body alignment consistent for default and explicitly aligned columns.

## Verification

- `npm run check` passes before release.
