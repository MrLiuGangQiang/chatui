# ChatUI v1.10.81

## 修复

- 修复多图（一次生成多张）任务在用户要求白色/黑色背景时被拦截的问题：之前 `image_plan.v1` 的 `background` 只允许 `auto/transparent/opaque`，模型按用户要求返回 `white` 会被拒绝，界面提示"路由任务尚未完成资源确认，已停止发送"。现在 `white/black` 成为合法背景值，白色/黑色背景的多图生成可以正常执行。
- 单图参数提取语义同步完善：排除"透明背景/不透明背景"后仍保持原有自动选择行为，其余歧义场景走澄清。
- 新增回归测试锁定该协议行为，防止再次出现。