# ChatUI v2.1.33

## Changes

- 修复 SSE 任务仍在运行、但切换会话回来或刷新恢复后页面暂时空白的问题；当会话中只有“正在处理 / 已等待”状态、尚未收到首段正文或思考时，恢复流程也会立即重放该状态，不再等待下一段 SSE。
- 保留会话隔离约束：后台任务仍只更新自己的会话，不会把状态或内容写入当前会话。

## Verification

- `npm run check`：项目、架构、语法检查及 2026 个测试全部通过。
- 新增“切回直播会话时在首个 delta 之前恢复等待状态”的回归测试。
- 已验证累计正文、累计思考和等待状态均能在切换或刷新后立即恢复。
