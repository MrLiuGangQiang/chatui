# ChatUI v1.10.83

## 修复

- 修复多图编辑在意图识别返回 `goal_mode: amend` 但当前没有上一次图片任务状态时报"意图模型返回了无效的任务结构"的问题：无旧任务可修改时，`amend` 自动降级为 `replace` 并保留目标描述，请求正常执行；有上一次任务状态时修改语义保持不变。
- 协议底层校验保持严格，容错仅在模型输出到路由的适配边界生效。
- 新增回归测试锁定该归一化行为，防止再次出现。