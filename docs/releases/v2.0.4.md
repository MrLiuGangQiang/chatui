# ChatUI v2.0.4

## 变更

- 意图识别失败提示改为平实用户语言：不再显示内部诊断措辞（“意图模型返回了无效的任务结构 / 本次未执行”），改为“AI 没理解这条请求，请重试；如果还无法理解，请切换更强模型。”，并新增对应回归测试。
- 修复 amend 校验与执行管线对“是否存在可继承前序任务状态”判定不一致导致的误判：旧版执行记录（resolved_goal/input）本可支撑“继续/再画”续作，却被校验器判为无基准、修复循环永远无法满足；现在校验器与管线统一使用同一推导，合法续作不再被误判为无效任务结构，减少“本次未执行”类失败，并新增回归测试。
