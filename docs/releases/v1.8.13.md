# ChatUI v1.8.13

This release completes the Node 20 CI compatibility fix for the Docker startup repair.

- Inject the missing `Iterator` global when jsdom creates Node 20 test VM contexts.
- Retain the production `undici` dependency and Docker dependency validation.
