# ChatUI v2.0.5

## 变更

- 修复：PID 文件生命周期回归。此前重构将 pid 文件逻辑抽到 server/pid-files.js 后，入口以空参调用 writePidFiles/removeOwnPidFiles，导致运行中的服务不写入、也不清理 pid 文件。现在正确传入解析出的 pid 文件列表，并新增回归测试（无修复时失败、有修复时通过）。
- 重构（行为不变）：移除 reasoning-workflow 中 22 处 legacy `with(deps)`，改为显式 `deps.*` 引用；`shared/clarification-answer.js` 的 `var` 统一为 `const/let`。均经全量测试与真实模型回归验证。
- 维护：架构基线记录 with-scope 债务 69 → 47（已完成 reasoning-workflow 的清理）。
