# ChatUI v1.10.30

## Changes

- 完成 ACR 发布流程修复：候选镜像禁用不兼容的 OCI 附加证明，正式标签逐个指向同一个已验证的单平台镜像 digest。
- 修复版本标签校验因 OCI index 包装导致的 digest 不一致问题，确保 `1.10.30`、`v1.10.30` 和 `latest` 使用同一已验证镜像。
- 整理相关变更与测试，保持发布流程、运行时校验和版本说明一致。
