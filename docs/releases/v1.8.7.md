# ChatUI v1.8.7

This hotfix ensures image edits remain on the returned image after a refresh.

- Persist the IndexedDB-backed image returned by a resumed image job, rather than its pre-edit source image.
- Keep the live image card, canonical message, and restored session context on the same returned result.
