# ChatUI v2.1.36

## Changes

- 修复重新生成时偶发出现空白助手消息：空的替换槽位不再创建 DOM，恢复时也不会作为有效等待项保留。
- 统一收口空替换消息判定与待恢复投影校验，只有正文、思考、富媒体或输出阶段证据有效的任务才会恢复到界面。

## Verification

- `npm run check`：项目、架构、语法检查及 2080 条测试通过。
- `npm run test:streaming`：40 条流式、恢复、背压、批量和容量测试通过。
- GitHub main CI：Node 20.19、Node 22 及 Exact Docker runtime 均通过。
- 发布候选继续通过 immutable candidate digest runtime、ACR/Docker Hub digest、GitHub Release 和 tag workflow 验证。
