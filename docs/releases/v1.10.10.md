# ChatUI v1.10.10

## 问题反馈

- 反馈输入框内置“问题描述、复现描述、期望结果”标准模板，并自动附带当前意图识别模型和聊天模型。
- 提交前由当前聊天模型检查反馈是否完整、合理；缺少必要信息时直接提示补充，不再发送无效反馈。
- 服务端会重新生成模型信息并剥离用户编辑的自动字段，确保审核内容和钉钉通知中的模型记录准确。

## 本地开发

- `npm start` 现在会读取 Git 忽略的 `.env.local`，便于本地连接 PostgreSQL 等服务，同时不会覆盖部署环境已经注入的变量。
