# ChatUI v1.8.10

This release fixes production Docker startup and Node 20 test compatibility.

- Include the server's `undici` HTTP dispatcher in production dependencies.
- Use a Node 20-compatible jsdom release for the test suite.
- Extend production-image dependency validation to cover `undici`.
