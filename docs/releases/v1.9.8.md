# ChatUI v1.9.8

This patch fixes the in-app version changelog in Docker deployments.

- Include release notes in the Docker image.
- Keep the Docker ignore rules narrow so `/api/changelog` can read the bundled release notes.
