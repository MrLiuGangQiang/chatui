# ChatUI v1.10.44

## Changes

- 图片生成、参考图生成和图片编辑现在会先把会话中的选择、建议或历史方案整理为完整的独立执行指令，避免将“按方案 A”“照你说的”等不完整引用直接发送给图片模型。
- 多图任务会在拆分前完成同样的指令整理；每个子任务只接收完整任务目标，不会混入未选择的方案内容。
- 当历史内容不足以确定用户选择时，系统会明确要求补充说明，而不是猜测后执行。
- 增强图片执行契约的前后端校验，阻止仍含未解析会话引用的提示词进入图片 provider。
