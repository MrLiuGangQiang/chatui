# ChatUI v1.8.1

## Fixes

- Preserved the text of an explicitly quoted message when generating a new image, even if the route model omitted the quoted-message resource from its contract.
- Combined the quoted description with the current image instruction before dispatching the generation request.

## Verification

- Added a regression test for omitted quoted-message bindings in `text_to_image` routes.
- `npm run check` passes with 519 tests.
