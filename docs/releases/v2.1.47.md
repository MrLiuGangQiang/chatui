# ChatUI v2.1.47

## Changes

- 修复使用统计和问题反馈非模态浮层无法通过点击外部关闭的问题。
- 点击卡片外区域、再次点击对应入口或使用卡片内关闭按钮均可收起浮层；点击卡片内部不会误关闭。
- 非模态行为保持不变，页面未被卡片覆盖的区域仍可正常操作。
- 更新前端缓存版本并补充自动关闭回归测试，防止后续修改重新引入无法关闭的问题。

## Verification

- `npm run check`: project, architecture and syntax checks passed; all 2225 tests across 373 files passed.
- 新增回归测试覆盖统计与反馈浮层的外部点击关闭、入口切换关闭、卡片内点击不误关闭，以及原有非模态穿透行为。
- 本机 Docker 不可用；推送候选提交后将等待 GitHub `Exact Docker runtime` 成功，再进行打标签与发布。
