# ChatUI v1.10.47

## Changes

- Fix flicker while hovering over a streaming reply by reserving the message-list scrollbar gutter before the scrollbar appears.
- Make regenerated historical replies follow the live reply itself rather than competing with the conversation-tail scroll lock, so streaming remains smooth.
- Make **Continue viewing output** return to the current streaming reply position instead of an unrelated fixed page location.
