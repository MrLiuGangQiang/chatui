# ChatUI v1.10.31

## Changes

- 修复 Docker Hub 镜像同步仍使用多标签 `imagetools create` 导致 OCI index 重包装、最终 digest 校验失败的问题。
- Docker Hub 的 `1.10.31`、`v1.10.31` 和 `latest` 标签现在逐个从已验证的 ACR digest 同步，并保持相同的单平台 manifest digest。
- 增加 Docker Hub 同步流程回归校验，确保 ACR 与 Docker Hub 的发布验证逻辑一致。
