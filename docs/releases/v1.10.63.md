# ChatUI v1.10.63

## Changes

- Improve intent recognition compatibility when the selected routing model is DeepSeek.
- Use JSON Object mode first for DeepSeek route recognition while retaining the route_intent.v3 schema as a prompt constraint.
- Keep local route validation and compilation unchanged, and preserve the existing JSON Schema-first behavior for other models.
- Refresh the browser compatibility cache key and add regression coverage for DeepSeek intent requests.
