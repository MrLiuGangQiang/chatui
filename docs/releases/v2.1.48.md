# ChatUI v2.1.48

## Changes

- 修复“图片 + 文件”联合问答被误判为只看图片或只读文件的问题。
- 当同一问题同时依赖当前图片和当前文件时，现在统一按多模态问答执行，回答会同时使用两类附件；模型若降级为单模态，会进入语义修复，修复失败则明确失败关闭，不会静默忽略其中一类附件。
- 同步更新意图识别决策链设计与架构说明，并补充对应回归测试。

## Verification

- `npm run check`: project, architecture and syntax checks passed; all 2227 tests across 373 files passed.
- 新增回归测试覆盖联合图片 + 文件请求不得降级为单模态操作，以及模型降级时进入修复并同时绑定图片与文件后才允许执行。
- 本机 Docker 不可用；推送候选提交后将等待 GitHub `Exact Docker runtime` 成功，再进行打标签与发布。