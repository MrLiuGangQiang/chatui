# ChatUI v1.7.28

## Workflow and routing reliability

- Strengthened the browser workflow boundaries so bootstrap, chat, media, session, and message behavior use explicit dependencies.
- Preserved canonical message and image state during streaming, session switching, history restoration, and regeneration.
- Made intent-routing resource bindings and context budgeting fail closed when required context cannot be recovered.
- Improved streaming Markdown finalization, image-edit request handling, route-model continuity, and task lifecycle recovery.

## Verification

- Started the local server and verified `http://127.0.0.1:8765/` returns HTTP 200.
- Ran `npm run check`: project, architecture, syntax, and all 463 automated tests passed.
