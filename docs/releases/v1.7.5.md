# ChatUI v1.7.5

Released on July 20, 2026.

## Improvements

- Replaced the embedded intent-routing diagram on empty sessions with a redesigned `ChatUI 奥哲AI小助手` welcome experience.
- Added concise highlights for intelligent intent routing, deep reasoning, multimodal creation, and continuous session recovery while preserving the existing usage notice.
- Reduced the welcome page footprint with a compact premium layout, denser capability cards, and a two-column mobile grid.
- Positioned the desktop welcome content within the available space above the composer while keeping short screens and mobile layouts safely top-aligned.
- Kept the standalone intent-routing diagram launcher available outside the welcome page.

## Reliability

- Encoded the welcome copy with ASCII-safe JavaScript Unicode escapes to prevent Chinese text corruption across PowerShell, static bundling, proxies, and immutable browser caches.
- Added regression coverage for welcome content, layout density, responsive positioning, cache versions, and encoding safety.

## Verification

- Verified the redesigned welcome page in Microsoft Edge at desktop, short-desktop, and mobile viewport sizes.
- Confirmed the rendered title and capability labels have the expected Unicode code points with no replacement characters or repeated question marks.
- `npm run check` passes with 368 tests.
