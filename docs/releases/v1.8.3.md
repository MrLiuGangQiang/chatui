# ChatUI v1.8.3

## 新增

- 无。

## 修复

- 添加了回归覆盖范围，证明路由语义永远不会在本地默默纠正，并且有效的首次通过决策不会调用修复或回退路由。

## 删除

- 删除了路由绑定、准备情况、编辑目标和缺少编辑详细信息的本地语义重写。

## 其他

- 使第一个“route_decision.v1”具有权威性，同时使编译器仅限于验证和机械合约映射。
- 加强了针对独立图像后续、明确引用的描述、不完整的后续和不明确的图像编辑的首次通过路由指导。
- Ensured “再画一只狗，换个品种” is routed with no historical message binding, while genuinely context-dependent requests bind their source on the first pass.
- `npm run check` 通过了 540 项测试。
