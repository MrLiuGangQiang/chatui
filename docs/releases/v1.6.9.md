# ChatUI v1.6.9

## Background task completion and quoted image prompt fixes

- Fixes completed background image jobs remaining stuck in the in-progress state after switching between conversations.
- Settles recovered chat and image jobs through the canonical task lifecycle before clearing transient recovery owners, keeping the composer, stop/send button, and session sidebar synchronized.
- Correctly finalizes recovered terminal failures while preserving recoverable state for transient transport interruptions.
- Preserves the full explicitly quoted message description when generating an image, even if intent routing classifies the request as a new task.
- Prevents short follow-up instructions such as "generate an image from the quoted message" from replacing detailed quoted scene, style, and composition requirements.

## Verification

- Added lifecycle regressions for completed background image recovery, canonical busy-state release, follower cleanup, and completed recovery snapshots.
- Added routing and prompt-composition coverage proving explicit quoted descriptions survive through the final image execution prompt without inheriting unrelated history.
- Verified the multi-conversation background image completion flow in a real browser: the completed image remains visible, the prompt is enabled, the send button returns to Send, and the session sidebar no longer shows an active answer.
- Verified the complete project check suite with 331 passing tests.
- Docker deployments can upgrade to the `1.6.9` or `v1.6.9` image tag.
