# ChatUI v1.8.3

## Authoritative first-pass routing

- Removed local semantic rewrites of route bindings, readiness, edit targets, and missing edit details.
- Made the first `route_decision.v1` authoritative while keeping the compiler limited to validation and mechanical contract mapping.
- Strengthened first-pass routing guidance for self-contained image follow-ups, explicitly referenced descriptions, incomplete follow-ups, and ambiguous image edits.
- Ensured “再画一只狗，换个品种” is routed with no historical message binding, while genuinely context-dependent requests bind their source on the first pass.

## Verification

- Added regression coverage proving that route semantics are never silently corrected locally and that valid first-pass decisions do not invoke repair or fallback routing.
- `npm run check` passes with 540 tests.
