# ChatUI v1.10.80

## 修复

- 修复使用千问（Qwen / DashScope）模型时频繁报错 `When using 'tool_choice', 'tools' must be set.` 的问题：意图识别、图片指令等一次性请求不再在未携带 `tools` 时发送 `tool_choice`，与 OpenAI 兼容网关（如千问）的严格参数校验对齐。
- 联网搜索等真正使用工具的场景仍保留 `tool_choice` 支持，行为不变。
- 新增回归测试锁定该参数组合规则，防止兼容性问题再次出现。