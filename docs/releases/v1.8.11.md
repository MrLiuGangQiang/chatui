# ChatUI v1.8.11

This follow-up release completes the Node 20 compatibility fix for the production Docker repair.

- Use jsdom 26.1.0, which does not require the Node 22 `Iterator` global in tests.
- Retain the production `undici` dependency and Docker validation from v1.8.10.
